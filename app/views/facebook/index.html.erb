This is a high scores page for War Games<br/>
<% User.all.each do |u| %>
  <%= 
    if(u.top_score != nil)
      score = u.top_score
    else
      score = u.curScore
    end
    "#{score} was scored by #{u.name}" %><br/>
<% end %>
